---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: virtualtargetproviders.kubetarget.kubetarget.dev
spec:
  group: kubetarget.kubetarget.dev
  names:
    kind: VirtualTargetProvider
    listKind: VirtualTargetProviderList
    plural: virtualtargetproviders
    singular: virtualtargetprovider
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VirtualTargetProvider is the Schema for the virtualtargetproviders
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VirtualTargetProviderSpec defines the desired state of VirtualTargetProvider
            properties:
              configuration:
                description: Configuration contains provider-specific configuration
                properties:
                  additionalConfig:
                    additionalProperties:
                      type: string
                    description: Additional provider-specific configuration
                    type: object
                  credentials:
                    description: Credentials contains provider-specific credentials
                    properties:
                      secretRef:
                        description: SecretRef is a reference to a Secret containing
                          credentials
                        type: string
                    type: object
                  resources:
                    description: Resources defines resource limits and requests
                    properties:
                      cpu:
                        description: CPU resource configuration
                        properties:
                          limit:
                            description: Limit specifies the CPU limit
                            type: string
                          request:
                            description: Request specifies the CPU request
                            type: string
                        type: object
                      memory:
                        description: Memory resource configuration
                        properties:
                          limit:
                            description: Limit specifies the memory limit
                            type: string
                          request:
                            description: Request specifies the memory request
                            type: string
                        type: object
                    type: object
                type: object
              image:
                description: Image specifies the container image for the provider
                type: string
              providerType:
                description: ProviderType specifies the type of provider (e.g., qemu,
                  corellium, avd)
                type: string
            required:
            - image
            - providerType
            type: object
          status:
            description: VirtualTargetProviderStatus defines the observed state of
              VirtualTargetProvider
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the provider's current state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              phase:
                description: Phase represents the current phase of the provider (Pending,
                  Running, Failed)
                type: string
              supportedHardwareTypes:
                description: SupportedHardwareTypes lists the hardware types supported
                  by this provider
                items:
                  type: string
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
